import {defineField, defineType} from 'sanity'
import {portableHeadlineField} from '../shared/inlineHeroHeadlineField'
import {
  CONTENT_SEO_FIELDSETS,
  CONTENT_SEO_GROUPS,
  sectionDividerFieldset,
  singletonSeoField,
} from '../shared/singletonContentFields'
import {DEFAULT_MEMBERSHIP_SPLASH_HEADING_PORTABLE} from '../../../shared/cms/inlineHeroHeadline'

export const paymentPage = defineType({
  name: 'paymentPage',
  title: 'Payment page (/membership)',
  type: 'document',
  groups: CONTENT_SEO_GROUPS,
  fieldsets: [
    sectionDividerFieldset('welcomeSplashDivider', 'Welcome splash'),
    sectionDividerFieldset('benefitsDivider', 'Benefits panel'),
    sectionDividerFieldset('pricingDivider', 'Plan pricing'),
    sectionDividerFieldset('promoDivider', 'Promo & discount'),
    sectionDividerFieldset('ctaDivider', 'Bottom CTA'),
    ...CONTENT_SEO_FIELDSETS,
  ],
  fields: [
    defineField({
      name: 'welcomeSplashEnabled',
      type: 'boolean',
      title: 'Show welcome splash',
      description:
        'Full-screen welcome animation when approved members land on /membership. Turn off to show the payment page immediately.',
      initialValue: true,
      group: 'content',
      fieldset: 'welcomeSplashDivider',
    }),
    portableHeadlineField({
      name: 'welcomeSplashHeadingPortable',
      title: 'Splash headline',
      initialValue: DEFAULT_MEMBERSHIP_SPLASH_HEADING_PORTABLE,
      group: 'content',
      fieldset: 'welcomeSplashDivider',
      hidden: ({parent}) => !parent?.welcomeSplashEnabled,
    }),
    defineField({
      name: 'welcomeSplashSubheading',
      type: 'text',
      rows: 3,
      title: 'Splash subheadline',
      initialValue:
        'Secure your spot in the Rellia network to finalize your membership and unlock your exclusive founder benefits.',
      group: 'content',
      fieldset: 'welcomeSplashDivider',
      hidden: ({parent}) => !parent?.welcomeSplashEnabled,
    }),
    defineField({
      name: 'welcomeSplashBackground',
      type: 'image',
      title: 'Splash background image',
      description: 'Upload the full-screen welcome photo.',
      options: {hotspot: true},
      group: 'content',
      fieldset: 'welcomeSplashDivider',
      hidden: ({parent}) => !parent?.welcomeSplashEnabled,
    }),
    defineField({
      name: 'welcomeSplashBackgroundSrc',
      type: 'string',
      title: 'Splash background URL (fallback)',
      description: 'Used when no image is uploaded (e.g. /images/membership-splash.jpg).',
      initialValue: '/images/membership-splash.jpg',
      group: 'content',
      fieldset: 'welcomeSplashDivider',
      hidden: ({parent}) => !parent?.welcomeSplashEnabled,
    }),
    defineField({
      name: 'welcomeSplashLogo',
      type: 'image',
      title: 'Splash logo',
      description: 'Circle network logo shown above the headline.',
      options: {hotspot: true},
      group: 'content',
      fieldset: 'welcomeSplashDivider',
      hidden: ({parent}) => !parent?.welcomeSplashEnabled,
    }),
    defineField({
      name: 'welcomeSplashLogoSrc',
      type: 'string',
      title: 'Splash logo URL (fallback)',
      description: 'Used when no logo image is uploaded.',
      initialValue: '/svgs/rellia-secondary-logo-circle-health-white-rgb.svg',
      group: 'content',
      fieldset: 'welcomeSplashDivider',
      hidden: ({parent}) => !parent?.welcomeSplashEnabled,
    }),
    defineField({
      name: 'welcomeSplashDurationSeconds',
      type: 'number',
      title: 'Splash hold after reveal (seconds)',
      description:
        'Seconds to pause after the headline and subheadline finish animating in, before the splash slides away (0.5–8).',
      initialValue: 1.9,
      validation: (Rule) => Rule.min(0.5).max(8),
      group: 'content',
      fieldset: 'welcomeSplashDivider',
      hidden: ({parent}) => !parent?.welcomeSplashEnabled,
    }),
    defineField({
      name: 'benefitsPanelHeadline',
      type: 'string',
      title: 'Left panel headline',
      description: 'Teal benefits card headline (e.g. Join the network today).',
      group: 'content',
      fieldset: 'benefitsDivider',
      validation: (Rule) => Rule.required(),
    }),
    defineField({
      name: 'benefitsTitle',
      type: 'string',
      title: 'Benefits title (fallback)',
      description: 'Used only if left panel headline is empty.',
      group: 'content',
      fieldset: 'benefitsDivider',
    }),
    defineField({
      name: 'benefitsPanelImageEnabled',
      type: 'boolean',
      title: 'Show background image on left panel',
      description: 'When off, the left panel uses a teal gradient only.',
      initialValue: true,
      group: 'content',
      fieldset: 'benefitsDivider',
    }),
    defineField({
      name: 'benefitsPanelImage',
      type: 'image',
      title: 'Left panel background image',
      description: 'Upload a background photo for the left membership panel.',
      options: {hotspot: true},
      group: 'content',
      fieldset: 'benefitsDivider',
      hidden: ({parent}) => !parent?.benefitsPanelImageEnabled,
    }),
    defineField({
      name: 'benefitsPanelImageSrc',
      type: 'string',
      title: 'Left panel background URL (fallback)',
      description: 'Used when no image is uploaded (e.g. /images/membership-splash.jpg).',
      initialValue: '/images/membership-splash.jpg',
      group: 'content',
      fieldset: 'benefitsDivider',
      hidden: ({parent}) => !parent?.benefitsPanelImageEnabled,
    }),
    defineField({
      name: 'benefitsPanelDescriptionPortable',
      type: 'membershipPanelPortableText',
      title: 'Left panel description',
      description:
        'Intro copy and bullet list for the membership benefits panel. Use normal paragraphs and bullet lists.',
      group: 'content',
      fieldset: 'benefitsDivider',
    }),
    defineField({
      name: 'benefitsPanelDescription',
      type: 'text',
      rows: 10,
      title: 'Left panel description (legacy)',
      description: 'Deprecated — migrated to rich text. Kept for legacy reads only.',
      group: 'content',
      fieldset: 'benefitsDivider',
      hidden: true,
      readOnly: true,
    }),
    defineField({
      name: 'choosePlanHeadline',
      type: 'string',
      title: 'Plan picker headline',
      initialValue: 'Choose your plan',
      group: 'content',
      fieldset: 'pricingDivider',
      validation: (Rule) => Rule.required(),
    }),
    defineField({
      name: 'pricingMonthlyAmount',
      type: 'string',
      title: 'Monthly price (e.g. $30)',
      group: 'content',
      fieldset: 'pricingDivider',
      validation: (Rule) => Rule.required(),
    }),
    defineField({
      name: 'pricingAnnualAmount',
      type: 'string',
      title: 'Annual price (e.g. $25)',
      group: 'content',
      fieldset: 'pricingDivider',
      validation: (Rule) => Rule.required(),
    }),
    defineField({
      name: 'pricingMonthlyDiscountEnabled',
      type: 'boolean',
      title: 'Monthly — show compare price',
      initialValue: false,
      group: 'content',
      fieldset: 'pricingDivider',
    }),
    defineField({
      name: 'pricingMonthlyCompareAmount',
      type: 'string',
      title: 'Monthly compare price',
      description: 'Strikethrough price shown when compare price is enabled (e.g. $40).',
      group: 'content',
      fieldset: 'pricingDivider',
    }),
    defineField({
      name: 'pricingAnnualDiscountEnabled',
      type: 'boolean',
      title: 'Annual — show compare price',
      initialValue: false,
      group: 'content',
      fieldset: 'pricingDivider',
    }),
    defineField({
      name: 'pricingAnnualCompareAmount',
      type: 'string',
      title: 'Annual compare price',
      description: 'Strikethrough price shown when compare price is enabled (e.g. $35).',
      group: 'content',
      fieldset: 'pricingDivider',
    }),
    defineField({
      name: 'monthlyProceedLabel',
      type: 'string',
      title: 'Monthly CTA label',
      group: 'content',
      fieldset: 'pricingDivider',
    }),
    defineField({
      name: 'annualProceedLabel',
      type: 'string',
      title: 'Annual CTA label',
      group: 'content',
      fieldset: 'pricingDivider',
    }),
    defineField({
      name: 'discountBannerEnabled',
      type: 'boolean',
      title: 'Show promo block',
      initialValue: true,
      group: 'content',
      fieldset: 'promoDivider',
    }),
    defineField({
      name: 'discountBannerBadge',
      type: 'string',
      title: 'Promo pill label',
      group: 'content',
      fieldset: 'promoDivider',
    }),
    defineField({
      name: 'discountBannerSubtitle',
      type: 'text',
      rows: 2,
      title: 'Discount code',
      description: 'Copied when user taps Apply code.',
      group: 'content',
      fieldset: 'promoDivider',
    }),
    defineField({
      name: 'promoMessage',
      type: 'text',
      rows: 2,
      title: 'Promo message',
      description: 'When set, the promo badge and message appear. Use {code} where the discount code should appear.',
      group: 'content',
      fieldset: 'promoDivider',
    }),
    defineField({
      name: 'questionsTitle',
      type: 'string',
      title: 'Bottom CTA headline',
      initialValue: 'Questions about membership?',
      group: 'content',
      fieldset: 'ctaDivider',
    }),
    defineField({
      name: 'questionsBody',
      type: 'text',
      rows: 3,
      title: 'Bottom CTA body',
      initialValue:
        "Have questions about the membership, billing, or benefits? We're here to help you get the most out of the Rellia network.",
      group: 'content',
      fieldset: 'ctaDivider',
    }),
    defineField({
      name: 'questionsContactLabel',
      type: 'string',
      title: 'Bottom CTA primary button label',
      initialValue: 'Contact us',
      group: 'content',
      fieldset: 'ctaDivider',
    }),
    defineField({
      name: 'questionsContactPath',
      type: 'string',
      title: 'Bottom CTA primary button path',
      initialValue: '/contact',
      group: 'content',
      fieldset: 'ctaDivider',
    }),
    defineField({
      name: 'questionsFaqLabel',
      type: 'string',
      title: 'Bottom CTA secondary button label',
      initialValue: 'View FAQ',
      group: 'content',
      fieldset: 'ctaDivider',
    }),
    defineField({
      name: 'questionsFaqPath',
      type: 'string',
      title: 'Bottom CTA secondary button path',
      initialValue: '/faq',
      group: 'content',
      fieldset: 'ctaDivider',
    }),
    singletonSeoField,
  ],
})
