import type { CareersOpenRole } from "./cms/types"

/** @deprecated Open roles are CMS documents (`openRole`). Kept for legacy imports only. */
export const CAREERS_OPEN_ROLES: CareersOpenRole[] = []
