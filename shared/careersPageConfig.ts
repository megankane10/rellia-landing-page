/**
 * Careers page content toggles (legacy static Careers.tsx).
 * Nav HIRING/VOLUNTEER badges and production open roles are CMS-driven on /careers (CareersCms).
 */
export const CAREERS_VOLUNTEER_ENABLED = true

/** @deprecated Route uses CareersCms; kept for legacy `Careers.tsx` only. */
export const careersHasPublishedOpenRoles = (): boolean => false
