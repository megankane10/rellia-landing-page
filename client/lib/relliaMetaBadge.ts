/** Primary tag — mint wash + teal text (profile pages). */
export const relliaProfilePrimaryTagClass =
  "inline-flex items-center rounded-full border border-rellia-teal/20 bg-rellia-mint/20 px-3 py-1 font-urbanist text-xs font-semibold text-rellia-teal"

/** Primary tag on directory cards — teal badge with mint text (matches production). */
export const relliaProfilePrimaryTagCardClass =
  "inline-flex rounded-full border border-rellia-teal/35 bg-rellia-teal/85 px-2.5 py-[3px] font-host-grotesk text-[9px] font-extrabold uppercase tracking-[0.08em] text-rellia-mint shadow-sm backdrop-blur-sm"

/** @deprecated Use relliaProfilePrimaryTagClass */
export const relliaPrimaryMetaBadgeClass = relliaProfilePrimaryTagClass

/** Secondary meta chip — neutral gray with optional leading icon. */
export const relliaSecondaryMetaBadgeClass =
  "inline-flex max-w-full items-center gap-1.5 rounded-full border border-black/10 bg-black/[0.02] px-3 py-1 font-urbanist text-sm text-black/65"
