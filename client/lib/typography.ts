/** Shared heading scale — use with `font-host-grotesk font-semibold` or `font-bold` as appropriate. */

/** Main landing page hero titles. */
export const HEADING_HERO = "text-[42px] sm:text-[46px] md:text-[68px] lg:text-[82px]"

/** Page hero titles (`PageHeader`, `RoleHero`). */
export const HEADING_PAGE = "text-4xl md:text-5xl lg:text-6xl"

/** Subtitle under page hero titles. */
export const HEADING_PAGE_SUBTITLE = "text-lg md:text-xl"

/** Subtitle under standard section titles. */
export const HEADING_SECTION_SUBTITLE = "text-base md:text-lg"

/** Paragraph/description text inside cards. */
export const TEXT_CARD = "text-sm sm:text-base"

/** Major landing page section titles (e.g. gateway selectors). */
export const HEADING_SECTION_LARGE = "text-3xl md:text-[38px] lg:text-[44px]"

/** Section titles (`SectionHeading`, in-page sections). */
export const HEADING_SECTION = "text-2xl md:text-[32px] lg:text-[36px]"

/** Card / bento item titles. */
export const HEADING_CARD = "text-xl md:text-2xl"

/** Bottom CTA band (`RelliaCta`) — default size. */
export const HEADING_CTA_BAND_DEFAULT = "text-4xl md:text-5xl lg:text-6xl"

/** Bottom CTA band — compact (FAQ, etc.). */
export const HEADING_CTA_BAND_COMPACT = "text-3xl md:text-4xl lg:text-[2.75rem]"
