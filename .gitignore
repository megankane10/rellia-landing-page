# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.pnpm-store/
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.config/
.env
.env.*
!.env.example

# Generated at build for prerender (requires SANITY_API_READ_TOKEN on Vercel Build)
shared/cms/build-snapshots/
.vercel

# esbuild output for Vercel serverless (`pnpm run build:api`; created on Vercel during build)

# Local contact form storage (append-only pretty JSON per submission). Folder stays visible; only the file is ignored.
data/contact-submissions.jsonl
.env*.local
